@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@{
	Layout = "~/Views/Shared/_Layout.cshtml";
	ViewData["Title"] = "Αλλαγή Password | kinsen.gr";
}
<style>
	#actionBtn:disabled {
		opacity: 0.5;
		cursor: not-allowed;
	}

	#password {
		width: 100%;
		box-sizing: border-box;
		padding-right: 45px;
	}

	#PasswordVisib {
		position: absolute;
		top: 38%;
		right: 22px;
		transform: translateY(-50%);
		background: none;
		border: none;
		color: #33333392;
		padding: 0;
		display: flex;
		align-items: right;
		justify-content: right;
		transition: all 0.2s ease;
		width: 20px;
	}

	#PasswordVisib .icon {
		font-size: 18px;
	}

	#password:focus,
	#password[type="text"],
	#password[type="password"] {
		width: 100%;
		box-sizing: border-box;
	}
</style>

<div class="login-wrapper">
	<div class="login-box">
		@* <img src="~/media/kinsenLogo.png" alt="Kinsen Logo" class="login-logo" /> *@
		<p class="forgetPass-title">Συμπληρώστε τον κωδικό που σας στάλθηκε <br /> στο email σας:</p>

		<div class="loginForm">
			<div class="mb-3">
				<label for="resetCode" class="form-label">Κωδικός Αποστολής:</label>
				<input type="text" id="resetCode" name="code" class="form-control" autocomplete="one-time-code" required/>			
  			</div>

			<div class="mb-3 new-pass">
				<label for="newPassword" class="form-label">Νέος Κωδικός:</label>
				<input type="password" id="newPassword" name="password" class="passwordVisib" autocomplete="current-password" class="form-control" required />
				<button type="button" class="toggle-btn" id="PasswordVisib">
					<span class="icon"><i class="fas fa-eye"></i> </span>
				</button>
			</div>

			<div class="mb-3 new-pass">
				<label for="confirmPassword" class="form-label">Επιβεβαίωση Κωδικού:</label>
				<input type="password" id="confirmPassword" name="password" class="passwordVisib" autocomplete="current-password" class="form-control" required />
				<button type="button" class="toggle-btn" id="PasswordVisib">
					<span class="icon"><i class="fas fa-eye"></i> </span>
				</button>
			</div>

			<button type="button" class="btn btn-primary" id="actionBtn" disabled onclick="submitNewPassword()">Αποθήκευση</button>
			<p id="statusMsg" class="status-msg"></p>
		</div>
	</div>
</div>

<script src="~/js/forgotPassword.js" defer></script>